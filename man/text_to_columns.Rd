% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text_to_columns.R
\name{text_to_columns}
\alias{text_to_columns}
\title{Convert Delimited Text into Indicator Columns}
\usage{
text_to_columns(
  x,
  sep = ";",
  items = NULL,
  other_name = "Other",
  other_sep = "|",
  na_col = NULL,
  ignore_case = FALSE
)
}
\arguments{
\item{x}{Character vector to parse into multiple columns.}

\item{sep}{Character string used to split multiple responses (default: \code{";"}).}

\item{items}{Optional character vector specifying expected items.
If provided, entries not matching these items are grouped into an "Other" column.}

\item{other_name}{Name for the "Other" column. Default: \code{"Other"}.}

\item{other_sep}{Separator used when collapsing unmatched values into the "Other" column. Default: \code{"|"}.}

\item{na_col}{Character string naming the missing-value column, or \code{NULL} to omit it.
For example, set \code{na_col = "Missing"} to include one.}

\item{ignore_case}{Logical; if TRUE, ignores letter case in value matching.}
}
\value{
A data frame where each specified or detected item becomes a numeric (0/1) column.
Optionally includes character \code{"Other"} and numeric \code{"Missing"} columns if requested.
}
\description{
Splits each entry in a character vector into binary (0/1) columns, useful for multiple-response questionnaire items.
Optionally supports "Other" and "Missing" columns.
}
\examples{
text_to_columns(c("A;B", "B", "C", NA))
text_to_columns(c("apples; pears", "pears; peaches", ""), sep = ";")
text_to_columns(c("A;B", "B;C;Z", "B"), items = c("A", "B", "C"))

}
